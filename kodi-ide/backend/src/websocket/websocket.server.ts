import { Server as HttpServer } from 'http';

export class WebSocketServer {
  constructor(private readonly _server: HttpServer) {}

  initialize(): void {
    // Websocket initialization placeholder
  }
}
